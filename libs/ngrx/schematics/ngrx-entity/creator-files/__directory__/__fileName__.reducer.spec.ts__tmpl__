import { initialChatState, reducer } from './<%= fileName %>.reducer';

describe('<%= className %> Reducer', () => {

  beforeEach(() => {});

  describe('unknown action', () => {
    it('should return the previous state', () => {
      const action = {} as any;

      const result = reducer(initial<%= className %>State, action);

      expect(result).toBe(initial<%= className %>State);
    });
  });
});

